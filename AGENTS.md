# **CRITICAL RULES**

- When reporting information to me, be extremely concise and sacrifice grammar for the sake of concision.
- if you have open questions, enumerate them to me and present options A(preferred)/B(alternative)/C(minimal)/D(creative).
- never invoke openspec binary directly, always use the `task ai:openspec -- ...` wrapper
